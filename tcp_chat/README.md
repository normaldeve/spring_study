# 🚀 실행 방법 (How to Run)

이 프로젝트는 C 언어 기반 TCP 클라이언트/서버 예제입니다.

서버를 먼저 실행한 뒤, 클라이언트를 실행해야 합니다.

## 📁 프로젝트 구조
```text
tcp_chat/
 ├─ server/
 │   └─ server.c
 └─ client/
     └─ client.c
```

## 1️⃣ 서버 실행
### 1. 서버 디렉토리로 이동

```bash
cd tcp_chat/server
```

### 2. 서버 컴파일

```bash
gcc server.c -o server
```

### 3. 서버 실행

```bash
./server
```

서버는 지정된 포트(예: 9000)에서 클라이언트의 접속을 대기합니다.

## 2️⃣ 클라이언트 실행

### 1. 클라이언트 디렉토리로 이동

```bash
cd tcp_chat/client
```

### 2. 클라이언트 컴파일

```bash
gcc server.c -o client
```

### 3. 서버 IP 주소 확인

클라이언트에서 접속할 서버 IP 주소는 아래 명령어로 확인할 수 있습니다.

```bash
ipconfig getifaddr en0
```

위 명령어는 macOS 기준이며,
출력된 값이 서버가 실행 중인 머신의 IP 주소입니다.

### 4. 클라이언트 실행
```bash
./client
```

### ⚠️ 주의 사항
- 서버는 항상 먼저 실행해야 합니다.
- 서버 코드에서는 INADDR_ANY를 사용하여 바인딩합니다.
- 서버와 클라이언트의 포트 번호는 반드시 동일해야 합니다.
- 동일한 PC에서 실행할 경우 127.0.0.1을 사용할 수 있습니다.